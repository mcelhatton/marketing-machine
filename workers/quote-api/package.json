{
  "name": "mdd-quote-api",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker hosting the MDD quote endpoints. Adapter only - logic lives in mdd-theme/mdd.functions/.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
